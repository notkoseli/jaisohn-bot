services:
  - type: web
    name: jaisohn-chatbot
    runtime: docker
    plan: starter
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PORT
        value: 5000
    disk:
      name: chroma-data
      mountPath: /app/chroma_db
      sizeGB: 1
